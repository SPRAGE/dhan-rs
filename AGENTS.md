# Codex Adapter

<!-- Generated from .ai/ by .ai/generators/compile.py. Do not edit directly. -->

Read `AI.md` and `.ai/instructions.md`. Read `.ai/methodology.md` only for Planned or Hard work; load other `.ai/context/` files only through those instructions.

Skills live in `.agents/skills/`; runtime roles live in `.codex/agents/`. Preserve `.codex/local/` and `.codex/tmp/`. If a mapped runtime capability is unavailable, execute it inline and state the limitation.
